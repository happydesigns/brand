---
title: Component system
description: The happydesigns interface layer for Nuxt UI components.
navigation:
  title: System
  icon: i-lucide-panels-top-left
---

Components carry the happydesigns system into product interfaces. This section documents how Nuxt UI components should feel once the brand layer is applied: structured, useful, calm, and precise.

This guide does not replace the Nuxt UI documentation. It explains brand behavior, component hierarchy, and which parts of the styling belong in the shared theme.

## Component Philosophy

- Start with useful behavior, accessible states, and clear hierarchy.
- Use borders, spacing, and semantic surfaces before decorative color.
- Keep controls compact, predictable, and easy to scan.
- Use coral for focus, active states, and signature emphasis.
- Keep one-off component styling out of pages unless the layout itself requires it.

## Component Families

::card-group
  :::card{title="Actions" icon="i-lucide-mouse-pointer-click" to="/docs/components/actions"}
  Buttons, badges, keyboard hints, and compact action hierarchy.
  :::

  :::card{title="Forms" icon="i-lucide-text-cursor-input" to="/docs/components/forms"}
  Inputs, selects, labels, help text, and form-level behavior.
  :::

  :::card{title="Feedback" icon="i-lucide-circle-check" to="/docs/components/feedback"}
  Alerts, progress, loading, empty states, success, warning, and errors.
  :::

  :::card{title="Navigation" icon="i-lucide-route" to="/docs/components/navigation"}
  Tabs, breadcrumbs, accordions, steppers, menus, and wayfinding.
  :::

  :::card{title="Data and content" icon="i-lucide-table-2" to="/docs/components/data-content"}
  Tables, cards, avatars, separators, prose, metadata, and structured content.
  :::

  :::card{title="Overlays" icon="i-lucide-panel-top-open" to="/docs/components/overlays"}
  Modals, slideovers, popovers, tooltips, dropdowns, and contextual surfaces.
  :::

  :::card{title="Layout and page" icon="i-lucide-panels-top-left" to="/docs/components/layout-page"}
  Page heroes, sections, grids, CTAs, links, and shell surfaces.
  :::

  :::card{title="Docs and prose" icon="i-lucide-book-open" to="/docs/components/docs-prose"}
  Markdown typography, callouts, code groups, cards, steps, and prose structure.
  :::

  :::card{title="System helpers" icon="i-lucide-settings-2" to="/docs/components/system-helpers"}
  Color mode, locale, auth, and error surfaces.
  :::

  :::card{title="Dashboard" icon="i-lucide-layout-dashboard" to="/docs/components/dashboard"}
  Sidebars, panels, toolbars, search, resize handles, and scroll areas.
  :::

  :::card{title="Publishing" icon="i-lucide-newspaper" to="/docs/components/publishing"}
  Blog, changelog, pricing, package comparison, and controlled motion.
  :::

  :::card{title="Chat and editor" icon="i-lucide-message-square-text" to="/docs/components/chat-editor"}
  Chat streams, prompts, reasoning, tools, and rich text editor surfaces.
  :::
::

## Theme Ownership

Shared component styling belongs in `app/app.config.ts`. Token and semantic surface behavior belongs in `app/assets/css/main.css`.

Docs pages should compose real Nuxt UI components and explain usage. They should not become the place where component colors, sizes, variants, or slot styles are redesigned one example at a time.

## Coverage Model

The Components section uses two documentation patterns:

| Pattern | Meaning |
| --- | --- |
| Focused example | A component or small set of similar components gets its own visible example, usage note, and Nuxt UI docs link. |
| Integrated pattern | Larger product families are shown as one realistic surface when the parts only make sense together. |

The goal is not to duplicate every Nuxt UI prop. The goal is to show how the happydesigns layer should behave when real Nuxt UI components are composed into product interfaces.

## Coverage Audit

::card{title="Component coverage" icon="i-lucide-list-checks" to="/docs/components/coverage"}
The component catalog and current coverage status for the brand layer.
::
